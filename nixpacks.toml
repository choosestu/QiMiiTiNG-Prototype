[phases.setup]
nixPkgs = ["bun", "nodejs_20"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["node ./node_modules/vinxi/bin/cli.mjs build"]

[start]
cmd = "node .output/server/index.mjs"
